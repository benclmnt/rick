{
  "private": true,
  "name": "bunny",
  "version": "1.0.0",
  "description": "Browser redirector",
  "main": "index.js",
  "scripts": {
    "conf-gen": "node generateConfig.js",
    "fmt": "prettier --write '**/*.{js,css,json,md}'",
    "start": "RUST_BACKTRACE=1 wrangler dev",
    "preview": "yarn conf-gen && wrangler preview --watch",
    "publish": "yarn conf-gen && wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bennett Clement <ben@benclmnt.com>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.15.0",
    "prettier": "^1.18.2",
    "url-parse": "^1.4.7",
    "yaml": "^1.10.0"
  },
  "dependencies": {}
}
